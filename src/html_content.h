#pragma once
#include <Arduino.h>

// This file is now generated from the Vue project build (webui/dist/index.html)
// To update, build the Vue project and copy the output here as a string literal.
// See webui/README.md for instructions.
const char WEB_HTML[] PROGMEM = R"rawliteral(
<!-- Paste built HTML here after building Vue project -->
)rawliteral";

class WebHTML {
public:
const char* getWebHTML();
};