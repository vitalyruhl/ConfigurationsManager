{
  "name": "esp32-config-webui",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "mock-server": "nodemon --watch db.json --exec \"json-server --watch db.json --port 32000\"",
    "bridge": "node deviceBridge.js",
    "bridge-dev": "npm-run-all --parallel bridge dev",
    "test": "npm-run-all --parallel mock-server dev"
  },
  "dependencies": {
    "version": "3.1.0",
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.1.12"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "npm-run-all": "^4.1.5"
  }
}
