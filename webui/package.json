{
  "name": "esp32-config-webui",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "mock-server": "nodemon --watch db.json --exec \"json-server --watch db.json --port 3000\""
  },
  "dependencies": {
    "vue": "^3.5.21"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^5.0.0",
    "nodemon": "^3.1.0"
  }
}
